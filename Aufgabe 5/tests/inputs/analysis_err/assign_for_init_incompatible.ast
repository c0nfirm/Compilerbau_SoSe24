(Program) {
    .items = [
        [0] = Func((FuncDef) {
            .return_type = void,
            .ident = "main",
            .params = [],
            .statements = [
                [0] = For((ForStmt) {
                    .init = VarDef((VarDef) {
                        .data_type = int,
                        .res_ident = (ResIdent) {
                            .ident = "i"
                        },
                        .init = Literal(Float(3.5))
                    }),
                    .cond = Literal(Bool(true)),
                    .update = (Assign) {
                        .lhs = (ResIdent) {
                            .ident = "i"
                        },
                        .rhs = Literal(Int(1))
                    },
                    .body = Empty()
                })
            ]
        })
    ]
}
